# ThinkStream streaming-video GRPO recipe — verl 0.4+.
#
# Inherits ppo_trainer defaults; overrides only what's specific to
# Qwen3-VL-8B + per-chunk multi-turn rollout + v12 reward.
#
# Launch:
#   bash recipe_thinkstream/run_thinkstream_grpo.sh
#   (or, from ThinkStream/, bash scripts/grpo_train_verl.sh)
#
# Streaming-video chunk-level rollout is wired via the custom AgentLoop
# in recipe_thinkstream/streaming_agent_loop.py (registered as
# `thinkstream_streaming_agent`). Per-chunk visual frame injection is
# enabled when `multi_turn.frames_root` (or env THINKSTREAM_FRAMES_ROOT)
# points at the pre-extracted JPEG frames directory.
hydra:
  searchpath:
    - pkg://verl.trainer.config

defaults:
  - ppo_trainer
  - _self_

data:
  max_prompt_length: 16384
  # v12.14 (2026-05-03): max_response_length is the TOTAL STITCHED length
  # across all chunk turns. Under the current `1 trajectory = 1 long
  # training sample` regime this is ALSO the actor-side per-sample
  # sequence length, which drives BOTH:
  #   (a) FSDP forward memory   (~ O(L²) attention even with use_remove_padding,
  #                              since flash_attn_varlen un-pads but does NOT
  #                              block-diagonalise across chunk boundaries)
  #   (b) ppo_max_token_len_per_gpu packing budget (a single sample longer
  #       than this can't fit in one micro-batch on one GPU)
  #
  # Capacity rule of thumb (Qwen3-VL-8B + FSDP zero3 + offload, 96GB H20):
  #   max_response_length=16384 → ~60 chunks (smoke / very short videos)
  #   32768 (DEFAULT) → ~120 chunks; covers all of current batch1's
  #                     60-95 chunk range + lower tier of batch2's
  #                     120-240s catalog tier. Safe with use_dynamic_bsz.
  #   65536 → ~180-240 chunks; tight, OOM possible at peak batch sizes
  #   >=131072 → 300+ chunks; STITCHED PATH NOT RECOMMENDED — switch to
  #              recurrent rollout (docs/v12.14_recurrent_design.md)
  #              which cuts the per-sample sequence to one chunk's
  #              prompt+response (~2-8K) regardless of trajectory length.
  max_response_length: 32768
  train_batch_size: 8
  return_raw_chat: True
  return_multi_modal_inputs: True
  filter_overlong_prompts: True
  truncation: left
  custom_cls:
    path: "recipe_thinkstream/thinkstream.py"
    name: CustomRLHFDataset

actor_rollout_ref:
  hybrid_engine: True
  model:
    # Qwen3-VL chat template with tool-calling protocol — matches
    # SYSTEM_PROMPT_V12 / TOOLS_SCHEMA in thinkstream.data.agent_protocol.
    # Reused verbatim from recipe/deepeyes/configs (Qwen2/3-VL share
    # this template family).
    custom_chat_template: "{% set image_count = namespace(value=0) %}{% set video_count = namespace(value=0) %}{%- if tools %}{{- '<|im_start|>system\\n' }}{%- if messages[0]['role'] == 'system' %}{%- if messages[0]['content'] is string %}{{- messages[0]['content'] }}{%- else %}{{- messages[0]['content'][0]['text'] }}{%- endif %}{%- else %}{{- 'You are a helpful assistant.' }}{%- endif %}{{- \"\\n\\n# Tools\\n\\nYou may call one or more functions to assist with the user query.\\n\\nYou are provided with function signatures within <tools></tools> XML tags:\\n<tools>\" }}{%- for tool in tools %}{{- \"\\n\" }}{{- tool | tojson }}{%- endfor %}{{- \"\\n</tools>\\n\\nFor each function call, return a json object with function name and arguments within <tool_call></tool_call> XML tags:\\n<tool_call>\\n{\\\"name\\\": <function-name>, \\\"arguments\\\": <args-json-object>}\\n</tool_call><|im_end|>\\n\" }}{% for message in messages %}{% if message['role'] != 'system' or loop.first == false %}{%- if (message.role == \"user\") or (message.role == \"system\" and not loop.first) or (message.role == \"assistant\" and not message.tool_calls) %}<|im_start|>{{ message['role'] }}\n{% if message['content'] is string %}{{ message['content'] }}<|im_end|>\n{% else %}{% for content in message['content'] %}{% if content['type'] == 'image' or 'image' in content or 'image_url' in content %}{% set image_count.value = image_count.value + 1 %}{% if add_vision_id %}Picture {{ image_count.value }}: {% endif %}<|vision_start|><|image_pad|><|vision_end|>{% elif content['type'] == 'video' or 'video' in content %}{% set video_count.value = video_count.value + 1 %}{% if add_vision_id %}Video {{ video_count.value }}: {% endif %}<|vision_start|><|video_pad|><|vision_end|>{% elif 'text' in content %}{{ content['text'] }}{% endif %}{% endfor %}<|im_end|>\n{% endif %}{%- elif message.role == \"assistant\" %}{{- '<|im_start|>' + message.role }}{%- if message.content %}{{- '\\n' + message.content }}{%- endif %}{%- for tool_call in message.tool_calls %}{%- if tool_call.function is defined %}{%- set tool_call = tool_call.function %}{%- endif %}{{- '\\n<tool_call>\\n{\"name\": \"' }}{{- tool_call.name }}{{- '\", \"arguments\": ' }}{{- tool_call.arguments | tojson }}{{- '}\\n</tool_call>' }}{%- endfor %}{{- '<|im_end|>\\n' }}{%- elif message.role == \"tool\" %}{%- if (loop.index0 == 0) or (messages[loop.index0 - 1].role != \"tool\") %}{{- '<|im_start|>user' }}{%- endif %}{{- '\\n<tool_response>\\n' }}{% if message['content'] is string %}{{ message.content }}{% else %}{% for content in message['content'] %}{% if content['type'] == 'image' or 'image' in content or 'image_url' in content %}{% set image_count.value = image_count.value + 1 %}{% if add_vision_id %}Picture {{ image_count.value }}: {% endif %}<|vision_start|><|image_pad|><|vision_end|>{% elif content['type'] == 'video' or 'video' in content %}{% set video_count.value = video_count.value + 1 %}{% if add_vision_id %}Video {{ video_count.value }}: {% endif %}<|vision_start|><|video_pad|><|vision_end|>{% elif content['type'] == 'text' or 'text' in content %}{{ content['text'] }}{% endif %}{% endfor %}{% endif %}{{- '\\n</tool_response>' }}{%- if loop.last or (messages[loop.index0 + 1].role != \"tool\") %}{{- '<|im_end|>\\n' }}{%- endif %}{%- endif %}{% endif %}{% endfor %}{%- else %}{% for message in messages %}{% if loop.first and message['role'] != 'system' %}<|im_start|>system\nYou are a helpful assistant.<|im_end|>\n{% endif %}{%- if (message.role == \"user\") or (message.role == \"system\" and not loop.first) or (message.role == \"assistant\" and not message.tool_calls) %}<|im_start|>{{ message['role'] }}\n{% if message['content'] is string %}{{ message['content'] }}<|im_end|>\n{% else %}{% for content in message['content'] %}{% if content['type'] == 'image' or 'image' in content or 'image_url' in content %}{% set image_count.value = image_count.value + 1 %}{% if add_vision_id %}Picture {{ image_count.value }}: {% endif %}<|vision_start|><|image_pad|><|vision_end|>{% elif content['type'] == 'video' or 'video' in content %}{% set video_count.value = video_count.value + 1 %}{% if add_vision_id %}Video {{ video_count.value }}: {% endif %}<|vision_start|><|video_pad|><|vision_end|>{% elif 'text' in content %}{{ content['text'] }}{% endif %}{% endfor %}<|im_end|>\n{% endif %}{%- elif message.role == \"assistant\" %}{{- '<|im_start|>' + message.role }}{%- if message.content %}{{- '\\n' + message.content }}{%- endif %}{%- for tool_call in message.tool_calls %}{%- if tool_call.function is defined %}{%- set tool_call = tool_call.function %}{%- endif %}{{- '\\n<tool_call>\\n{\"name\": \"' }}{{- tool_call.name }}{{- '\", \"arguments\": ' }}{{- tool_call.arguments | tojson }}{{- '}\\n</tool_call>' }}{%- endfor %}{{- '<|im_end|>\\n' }}{%- elif message.role == \"tool\" %}{%- if (loop.index0 == 0) or (messages[loop.index0 - 1].role != \"tool\") %}{{- '<|im_start|>user' }}{%- endif %}{{- '\\n<tool_response>\\n' }}{% if message['content'] is string %}{{ message.content }}{% else %}{% for content in message['content'] %}{% if content['type'] == 'image' or 'image' in content or 'image_url' in content %}{% set image_count.value = image_count.value + 1 %}{% if add_vision_id %}Picture {{ image_count.value }}: {% endif %}<|vision_start|><|image_pad|><|vision_end|>{% elif content['type'] == 'video' or 'video' in content %}{% set video_count.value = video_count.value + 1 %}{% if add_vision_id %}Video {{ video_count.value }}: {% endif %}<|vision_start|><|video_pad|><|vision_end|>{% elif content['type'] == 'text' or 'text' in content %}{{ content['text'] }}{% endif %}{% endfor %}{% endif %}{{- '\\n</tool_response>' }}{%- if loop.last or (messages[loop.index0 + 1].role != \"tool\") %}{{- '<|im_end|>\\n' }}{%- endif %}{%- endif %}{% endfor %}{%- endif %}{% if add_generation_prompt %}<|im_start|>assistant\n{% endif %}"
    use_remove_padding: True
    use_fused_kernels: True
  rollout:
    name: vllm
    mode: async
    n: 8
    tensor_model_parallel_size: 2
    gpu_memory_utilization: 0.55
    max_num_batched_tokens: 32768
    enforce_eager: True
    free_cache_engine: True
    enable_chunked_prefill: True
    # v12.13 (2026-05-02): vLLM cache configuration for streaming video.
    #
    # Two independent caches matter here, doing DIFFERENT things:
    #
    #   enable_prefix_caching (KV blocks on GPU):
    #     Reuses attention KV across requests with byte-identical prompt
    #     prefixes. Only meaningful WITHIN one trajectory's consecutive
    #     chunks (across trajectories there's no shared prefix). Hit rate
    #     for the text portion (system + memory's leading thinks) is
    #     low-to-moderate; hit rate for the visual portion is ~0% under
    #     the default sliding window because frame token IDs shift every
    #     chunk. Visual is NOT the target of this cache for our workload.
    #
    #   mm_processor_cache_gb (CPU-side image preprocessor cache):
    #     Caches the (PIL load + smart_resize + ViT-friendly tensor) for
    #     each unique (frame_path, min_pixels, max_pixels) key. Streaming
    #     video's sliding window means the same frame appears in N
    #     consecutive chunks (N = visual_window_chunks=16) so this cache
    #     hits ~(N-1)/N ≈ 94% on the visual side. THIS is the cache that
    #     actually saves rollout time for streaming video — ViT encoding
    #     dominates the per-chunk cost. pass2 uses --mm-processor-cache-gb
    #     512 and observes 93.8% hit. RL needs the same mechanism.
    #
    # `mm_processor_cache_gb` is forwarded to vLLM via engine_kwargs in
    # run_thinkstream_grpo.sh (Hydra +syntax for un-declared schema keys).
    # Default sized for 8-rollout × 16-chunk window × ~2MB/frame headroom.
    enable_prefix_caching: True
    response_length: 16384
    prompt_length: 16384
    # v12.13 (2026-05-02): cap vLLM's max_model_len explicitly. Without
    # this verl/workers/rollout/vllm_async_server.py:_validate_configs
    # auto-defaults to HF max_position_embeddings, which for Qwen3-VL-8B
    # is 262144 → vLLM startup profiler runs a 262K-token dummy + tokenizer
    # warns at HF model_max_length 16384 ("148634 > 16384"). The dummy
    # also chews enormous KV at the wrong size, which is the root of the
    # downstream IndexError observed at startup.
    #
    # Per-turn budget under v12.12 RUNTIME profile:
    #   compress turn (worst): 5K input + 4K output = 9K
    #   obs+recall (worst):   15K input + 1K output = 16K
    # max_model_len 32K leaves 2× headroom for tokenizer drift / KV planner.
    # Each chunk is an INDEPENDENT generate (recipe header line 11-44),
    # so we don't need 360 × per_turn — only the single largest turn.
    max_model_len: 32768
    agent:
      # Registers the chunk-level streaming loop. `name` matches what
      # CustomRLHFDataset writes to row["agent_name"]; verl dispatches by
      # that key.
      agent_loop_config_path: recipe_thinkstream/configs/agent_loops.yaml
    multi_turn:
      enable: True
      # v12.13: max_turns removed — verl MultiTurnConfig only accepts
      # max_assistant_turns/max_user_turns.
      # v12.14 (2026-05-03): default lowered 360 → 120 to match the
      # stitched-training capacity (see top-of-file max_response_length
      # comment + run_thinkstream_grpo.sh MAX_TURNS=120). Other launchers
      # (debug_verl_2gpu.sh / grpo_train_verl.sh) override via CLI; this
      # default catches direct `python -m verl.trainer.main_ppo` invocations.
      # 240+ requires v12.14 recurrent (docs/v12.14_recurrent_design.md).
      max_assistant_turns: 120
      max_user_turns: 120
      max_parallel_calls: 1
      # ── ThinkStream-specific keys (frames_root / frames_per_chunk /
      # visual_window_chunks / recall_stub_text) are NOT accepted by
      # verl's MultiTurnConfig. They're read by ThinkStreamStreamingAgentLoop
      # via environment variables (THINKSTREAM_FRAMES_ROOT etc.) — see
      # streaming_agent_loop.py:316 for the env-var fallback path. Set
      # them in the launch script before invoking the trainer:
      #   export THINKSTREAM_FRAMES_ROOT=/path/to/frames
      #   bash recipe_thinkstream/run_thinkstream_grpo.sh
      # Defaults (frames_per_chunk=2, visual_window_chunks=16) are baked
      # into the recipe's __init__ from scripts.agent_data_v5.config.

algorithm:
  adv_estimator: grpo
  use_kl_in_reward: False
  kl_ctrl:
    kl_coef: 0.0

# verl 0.4 reward config (new schema under top-level `reward`, not the
# legacy `reward_model`). The built-in NaiveRewardManager in
# verl.experimental.reward_loop.reward_manager already merges
# tool_extra_fields (the agent loop's extra_fields) into compute_score's
# extra_info — that's how `ts_chunk_kinds`, `ts_chunk_asst_texts` etc.
# reach our compute_score. The new framework only places ONE scalar at
# the trajectory's last assistant token, so per-chunk shaping is folded
# into compute_score's returned `score` (GDPO α-mix of outcome + state_avg)
# rather than broadcast to per-chunk token positions.
reward:
  num_workers: 8
  custom_reward_function:
    path: "recipe_thinkstream/thinkstream.py"
    name: compute_score
  reward_manager:
    source: register
    name: naive
